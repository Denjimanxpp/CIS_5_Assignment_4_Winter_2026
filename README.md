Here is the video for the code explaination: https://youtu.be/veMy2JcBLqc 

I used a few functions being the void, Calculate average and sum, minimum, and count high performers. 
I kept the int main to have the array and to put the array to work. 
It first printed up the original scores and then the pointers being scores[] were used to send towards calc sum. 
Afterwards, it returned sum. Same thing with the Average which it used the sum to calculate the average. 
For the void function it sent the information onto another line which printed out the info.
The final function being the count high performers which used a for loop to see if each element was higher than 85 then it was considered as a high performer. 
After all of that, the function returned all of the values. 
